#ifndef ERRORS_H
#define ERRORS_H

/**
 * Error codes for different SDL Modules so debugging is easier if
 * the program fails.
 */
enum SDL_ERROR_CODES
{
    SDL_INIT_ERROR = 73501,
    SDL_WINDOW_ERROR = 73502
};

#endif //ERRORS_H